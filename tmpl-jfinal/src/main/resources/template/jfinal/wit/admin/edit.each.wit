<%

var entity = currtable.sqlName;
writeWebFile(false, "WEB-INF/templates/admin/"+entity+"/edit.wit", #{
%>\<% layout(function(){ %>
<h1>${currtable.remarks}管理 ---&gt; 修改${currtable.remarks}
</h1>
<div class="form_box">
    <form action="\${BASE_PATH}/admin/${entity}/update" method="post">
        \<% include "_form"; %>
    </form>
</div>
\<% }); %>
<% }); %>
