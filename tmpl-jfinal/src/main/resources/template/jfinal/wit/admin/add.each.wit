<%

var entity = currtable.sqlName;
writeWebFile(false, "WEB-INF/templates/admin/"+entity+"/add.wit", #{
%>\<% layout(function(){ %>
<h1>${currtable.remarks}管理 ---&gt; 创建${currtable.remarks}
</h1>
<div class="form_box">
    <form action="\${BASE_PATH}/admin/${entity}/save" method="post">
        \<% include "_form"; %>
    </form>
</div>
\<% }); %>
<% }); %>